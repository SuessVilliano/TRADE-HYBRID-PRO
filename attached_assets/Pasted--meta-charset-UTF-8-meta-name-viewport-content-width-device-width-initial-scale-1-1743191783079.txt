


    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Trade Hybrid Coin (THC) - All-In-One Platform</title>
    <style>
        /* Global Styles */
        body {
            background-color: #0a0a23;
            color: #ffffff;
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
        }
        
        header {
            background-color: #1f1f3b;
            padding: 20px;
            text-align: center;
        }
        
        h1, h2 {
            margin: 0;
            color: #00ffdd;
        }

        nav {
            display: flex;
            justify-content: center;
            background-color: #1a1a3d;
            padding: 10px;
        }

        nav a {
            margin: 0 20px;
            text-decoration: none;
            color: #ffffff;
            font-size: 1.1em;
        }

        .container {
            padding: 20px;
            max-width: 1200px;
            margin: auto;
        }

        /* Sections */
        .section {
            margin: 20px 0;
            padding: 20px;
            background-color: #1f1f3b;
            border-radius: 10px;
        }

        .section h2 {
            color: #00ffdd;
        }

        .wallet-connect {
            display: flex;
            justify-content: center;
            align-items: center;
        }

        .wallet-button {
            padding: 10px 20px;
            font-size: 1.2em;
            color: #ffffff;
            background-color: #00ffdd;
            border: none;
            cursor: pointer;
            border-radius: 5px;
            margin: 10px;
        }

        /* Dashboard */
        .dashboard-content {
            display: flex;
            flex-wrap: wrap;
            justify-content: space-around;
        }

        .dashboard-item {
            background-color: #2a2a4d;
            padding: 20px;
            margin: 10px;
            border-radius: 10px;
            width: 45%;
        }

        /* Referral Program */
        .referral-input {
            width: 80%;
            padding: 10px;
            margin: 10px;
            border-radius: 5px;
        }

        .button {
            padding: 10px;
            background-color: #00ffdd;
            color: #1f1f3b;
            border: none;
            border-radius: 5px;
            cursor: pointer;
        }
    </style>


    <!-- Header -->
    <header>
        <h1>Trade Hybrid Coin (THC) Platform</h1>
    </header>
    
    <!-- Navigation Bar -->
    <nav>
        <a href="#home">Home</a>
        <a href="#dashboard">Dashboard</a>
        <a href="#referrals">Referral Program</a>
        <a href="#transactions">Coin Transactions</a>
        <a href="#staking">Staking</a>
        <a href="#exchange">Exchange</a>
        <a href="#resources">Resources</a>
    </nav>
    
    <!-- Main Content -->
    <div class="container">
        <!-- Home Section -->
        <div id="home" class="section">
            <h2>Welcome to the Trade Hybrid Coin Platform</h2>
            <p>Your all-in-one crypto platform for trading, staking, and more. Join the global movement to revolutionize the way we trade!</p>
            <div class="wallet-connect">
                <button class="wallet-button" id="connectWallet">Connect Wallet</button>
            </div>
            <div class="wallet-status" id="walletStatus">Wallet not connected</div>
        </div>
        
        <!-- Dashboard Section -->
        <div id="dashboard" class="section">
            <h2>Dashboard</h2>
            <div class="dashboard-content">
                <div class="dashboard-item">
                    <h3>Wallet Balance</h3>
                    <p id="walletBalance">0 THC</p>
                </div>
                <div class="dashboard-item">
                    <h3>Staked Balance</h3>
                    <p id="stakedBalance">0 THC</p>
                </div>
                <div class="dashboard-item">
                    <h3>Transaction History</h3>
                    <ul id="transactionHistory">
                        <li>No transactions yet.</li>
                    </ul>
                </div>
                <div class="dashboard-item">
                    <h3>Referral Rewards</h3>
                    <p id="referralRewards">0 THC</p>
                </div>
            </div>
        </div>

        <!-- Referral Program Section -->
        <div id="referrals" class="section">
            <h2>Referral Program</h2>
            <p>Share your referral link and earn rewards!</p>
            <input type="text" class="referral-input" id="referralLink" readonly="" value="https://yourwebsite.com/referral/USER_ID">
            <button class="button" onclick="copyReferralLink()">Copy Link</button>
        </div>

        <!-- Coin Transactions Section -->
        <div id="transactions" class="section">
            <h2>Coin Transactions</h2>
            <p>Send or receive THC tokens.</p>
            <button class="button" onclick="openTransactionModal()">Send THC</button>
        </div>

        <!-- Staking Section -->
        <div id="staking" class="section">
            <h2>Staking</h2>
            <p>Stake your THC coins and earn rewards.</p>
            <button class="button" onclick="openStakingModal()">Stake Now</button>
        </div>

        <!-- Exchange Section -->
        <div id="exchange" class="section">
            <h2>Exchange</h2>
            <p>Swap your THC coins with USDC or other cryptocurrencies.</p>
            <button class="button" onclick="openExchangeModal()">Open Exchange</button>
        </div>

        <!-- Resources Section -->
        <div id="resources" class="section">
            <h2>Educational Resources</h2>
            <p>Learn about trading, staking, and crypto best practices.</p>
            <button class="button" onclick="openResources()">Explore Resources</button>
        </div>
    </div>

    <!-- JavaScript for Basic Functionality -->
    <script type="vbout-nosrpt">
        let connectedWalletAddress = '';

        // Wallet connection logic
        async function connectWallet() {
            try {
                if (window.solana && window.solana.isPhantom) {
                    const response = await window.solana.connect();
                    connectedWalletAddress = response.publicKey.toString();
                    document.getElementById('connectWallet').innerText = 'Wallet Connected';
                    document.getElementById('walletStatus').innerText = `Connected: ${connectedWalletAddress}`;
                    // Fetch wallet balance, referral rewards, etc.
                } else {
                    alert('Solana wallet not found. Please install Phantom Wallet.');
                }
            } catch (error) {
                console.error('Wallet connection error:', error);
            }
        }

        // Copy referral link
        function copyReferralLink() {
            const referralLink = document.getElementById('referralLink');
            referralLink.select();
            document.execCommand('copy');
            alert('Referral link copied!');
        }

        // Placeholder functions for modals
        function openTransactionModal() {
            alert('Transaction modal coming soon!');
        }

        function openStakingModal() {
            alert('Staking modal coming soon!');
        }

        function openExchangeModal() {
            alert('Exchange modal coming soon!');
        }

        function openResources() {
            alert('Resource page coming soon!');
        }

        // Initialize wallet connection button
        document.getElementById('connectWallet').addEventListener('click', connectWallet);
    </script>

